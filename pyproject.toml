[project]
name = "rm-watermark"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.0.3",
    "opencv-python-headless>=4.9.0.80",
    "numpy>=1.26.4",
]

[tool.vercel]
entrypoint = "api.process:app"
